- if controller_name != 'sessions'
  = link_to "Вход", new_session_path(resource_name), class: 'label default button'

- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "Регистрация", new_registration_path(resource_name), class: 'label default button'
